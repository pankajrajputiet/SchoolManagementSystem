apiVersion: v1
kind: Service
metadata:
  name: sms-backend
  namespace: school-management
  labels:
    app: sms-backend
    tier: api
spec:
  type: ClusterIP
  selector:
    app: sms-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      name: http
